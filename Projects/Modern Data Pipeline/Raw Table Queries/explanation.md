# Modern Data Pipeline: Raw Table Queries

This repository contains a several SQL queries structured for use in Google's BigQuery enviroment. These queries can be used to 'create' or 'replace' tables with the correct schema for the fields pulled via the api.

Important to note, the Python script in the data ingestion step is designed to handle this step. I included these queries as examples of what can be used to achieve the same result.